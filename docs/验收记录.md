# 实施与验收记录

日期：2026-09-18。完整 V1 **未完成**，当前为可运行开发版本。

最新本机完整回归：**79 项通过**，已开启 4B Agent、Graphiti、Mem0、Docling、Embedding 与基础服务真实集成开关。后文保留此前失败及修复记录；这不是 iOS 真机或生产隔离验收。

| 子系统 | 已实现 | 验证与缺口 |
|---|---|---|
| 身份与会话 | 一次性配对、设备证明、防重放、访问/刷新令牌、设备撤销、成员邀请 | 自动测试；二维码扫描 UI 已实现但未真机验证；密钥轮换、真机 Secure Enclave 未验收 |
| 数据存储 | PostgreSQL、RLS、载荷加密、版本、共享撤回、墓碑、文件上传 | 真实 PostgreSQL 测试通过；固定分页快照、设备确认和批次幂等已通过真实验证；系统后台调度与规模验收待补齐 |
| 记忆 | 候选确认、规范事实、版本基础、授权范围文字检索 | 真实 EmbeddingGemma + pgvector 检索、版本重建、隔离与删除通过；Mem0 对账、检索、删除真实集成已通过；Graphiti 真实模型集成已通过，复杂冲突模型仍未完成 |
| 任务与 Agent | 持久化显式多步骤、结果引用、逐步审批、幂等、取消、重启恢复和人工核对 | 本地 llama.cpp 真实调用通过；本地自主多轮与 Token 预算已验证；云端费用预算、更多工具、条件及补偿待补齐 |
| 云模型与隐私 | OpenAI 兼容适配、公开资料固定指令、敏感内容拒绝、披露记录 | 无云账户实测；NER、本地复核、映射还原、完整脱敏未完成 |
| 事件与自动化 | Outbox、JetStream 发布、多步骤 Cron、停用 | 真实发布通过；持久消费者、事件触发、Skill 条件与补偿待补齐 |
| Provider | llama.cpp/MLX/vLLM 协议适配，Docling/FunASR/whisper/CosyVoice/Mem0/Graphiti/邮件/MCP 适配源码 | llama.cpp 与 Docling 七格式已做真实集成；Docling SDK/模型已锁定；其余 Provider 仍待真实验收 |
| 插件生命周期 | 注册、停用、配置回滚、开发启用 | 不含生产容器生命周期、签名、SBOM、gVisor、网络隔离；生产启用被阻止 |
| iOS | 五页界面、安全连接、聊天任务查询、审批、数据管理、单提醒自动化、部分数据连接器 | 模拟器构建与启动通过；语音录制/转写入口、二维码扫描、单次位置入口已有代码；真机与 Provider 联调、后台同步、App Intent/APNs 未完成 |
| 运维 | 开发 Compose、迁移账号分离、TLS、备份加密/认证、删除重放函数、CI 配置 | 开发服务已运行；本机加密备份及恢复到全新隔离数据库已通过；定时备份、异机恢复、生产部署、RPO/RTO 未验收 |

## 实际环境

- Apple Silicon Mac，Xcode 27.0；iPhone 17 Pro / iOS 27.0 模拟器已安装并启动应用。
- PostgreSQL 17 + pgvector、NATS 2.11、OPA 1.4.2 在 Docker 中实际运行，仅绑定本机端口。
- llama.cpp 8460，官方 Qwen3-0.6B-Q8_0 GGUF；仅为链路验证模型，不作为最终质量承诺。
- `homeai_runtime` 为实际业务库；`homeai_test` 为集成测试库。最初的 `homeai` 保留早期测试数据，没有删除。
- 本机启动地址：API `https://localhost:58443`，模型 `http://127.0.0.1:58080`。

## 测试证据

上一阶段测试集实际结果：**41 项通过**（含真实 PostgreSQL/OPA/NATS、llama.cpp 与 EmbeddingGemma）。真实 HTTPS 配对 → 签名请求 → worker → 模型回答也已通过，验收设备随后已撤销。

运行记录见 `logs/live-smoke.log`、`logs/full-validation.log`、`logs/integration.log`、`logs/model-integration.log`、`logs/ios-build.log`。单元测试覆盖重放、撤销、跨成员/家庭、共享撤回、版本冲突、墓碑、加密 AAD、审批与幂等、策略不可用、云端拒绝、模型参数边界、记忆确认与备份损坏。

本机恢复演练已生成 `state/backups/development-validation.haib` 并恢复到独立 `homeai_restore_validation_20260917`，未覆盖运行库。此次样本很小且没有真实附件，不能证明大规模恢复性能。

这些测试不能替代外部 Provider、真机、完整威胁模型和生产恢复验收。初始公开提交的 GitHub Actions 服务端与 iOS 作业已实际通过；后续提交单独以其 CI 结果为准。

## 后续完成所需

当前仍有上述未完成代码，不仅是缺少凭据。外部资源还包括最终服务器、iPhone 签名团队、云端地址/凭据、APNs、Home Assistant 实体、邮件账户、其余本地模型与异机备份位置。密钥只能通过本机私有配置或安全输入，不应粘贴到聊天。

## 网页与远程连接增量

- 已发布公开仓库，并加入自定义宽松归属许可、贡献说明与第三方组件说明。
- React/Vite 管理后台完成构建及浏览器登录验收；初始凭据、TOTP、CSRF、登录限流与会话撤销有自动测试。敏感操作增加短期重新认证。
- 本地远程客户端支持实例密钥、一次性绑定、租约续期、frpc 启动和失败关闭；拒绝非 HTTPS 或与平台确认域名不一致的绑定地址。
- 闭源远程平台部署在独立目录，代码、凭据和数据库未进入公开仓库。
- 实际外网 HTTPS 透传验收通过，使用精确固定的本机自签名证书；撤销后本次已有连接约 3.09 秒关闭，实际流量计数为 5830 字节。该结果不是性能 SLA。
- 家庭域名自动受信任证书申请/续期、iOS 真机远程访问、完整多步骤 Agent、隐私检测与生产沙箱仍未完成。
- iOS 新配对记录增加 TLS 公钥固定，允许同公钥且通过系统信任验证的证书续期；旧配对仍保留原证书固定，不静默替换信任。

- iOS 新增 3 项原生证书测试：有效证书接受、过期证书拒绝、尚未生效证书拒绝；已在模拟器实际通过，未修改系统信任设置。签名请求不跟随 HTTP 重定向。


## 2026-09-17 记忆链路补齐

- 本机下载固定版本 EmbeddingGemma Q8_0，官方 LFS SHA256 与实际文件一致。使用独立 llama.cpp 服务生成真实 768 维向量。
- 新增 `test_real_embedding.py`，不使用 HTTP Mock；验证真实 Embedding、OPA、PostgreSQL 的生成/检索链路、成员隔离、事实更新、模型版本切换、主动重建和删除。
- 索引不再依赖一次性 Outbox 消费标记：按规范记录版本和模型配置签名对账。更新后的旧向量立即退出检索，切换模型自动补建；不接受零向量、布尔值、NaN 或 Infinity。
- Agent 的 `memory.search@v1` 接入同一向量检索路径，失败时返回明确的授权文字检索模式。
- 管理后台新增真实索引状态与重建入口；OpenAPI 和 Manifest Schema 同步更新。
- Mem0/Graphiti 消费者代码已接入主体清除、规范账本重建、检查点和失败重试；未同步投影禁止搜索。**本轮尚未完成两个 SDK 与本地模型/Neo4j 的真实集成，仍不得标为 Provider 验收通过。**
- 完整 V1 仍未完成：多步骤 Agent、完整隐私检测、事件 Skill、生产沙箱、其余 Provider、iOS 后台/APNs/App Intent、家庭域名 ACME、真机与异机恢复继续待办。


## 2026-09-17 多步骤执行与恢复

- 显式工作流支持最多 16 步、步骤预算、前序结果引用、任务截止时间、外部调用超时和有限只读重试；每步独立持久化并检查策略及审批。
- PostgreSQL 连接级任务锁覆盖网络调用与事务提交，避免多 worker 重复执行；启动不再批量将其他进程中的任务标为未知。
- 已完成步骤不重放；不明外部副作用只能人工核对。核对依据加密保存，浏览器要求近期密码/TOTP；确认未执行后高风险步骤重新审批，过期或已取消任务禁止重新执行。
- 真实 PostgreSQL/OPA 测试覆盖步骤结果引用、锁竞争、步骤边界恢复、审批取消、过期未知结果及 TCP 连接失败重试预算。未通过测试实际发送邮件，不把外部发送当作已验证。
- 网页任务列表新增步骤详情、取消与人工核对；Cron 使用相同多步骤工作流。
- 自然语言自主多轮 Agent、费用预算、Skill 条件/补偿仍未完成；完整目标保持进行中。

本轮最终回归为 36 项通过，包含真实工具结果进入真实 llama.cpp 的多步骤流程和过期审批收尾；管理后台构建通过。


## 2026-09-17 本地自主多轮 Agent

- 本地自然语言任务采用持久化规划与工具反馈循环，一次规划可产生多条经校验的工具步骤；已执行步骤不重放。
- 增加工具数量、规划轮次、截止时间和保守 Token 预留预算；预算不足时在网络调用前拒绝。未知消耗不返还。
- 工具结果中的规范记录再次授权读取，秘密、删除、撤权数据不能继续进入规划上下文；无本地模型时失败，不切换云端。
- 真实 llama.cpp、PostgreSQL 与 OPA 验证模型自主创建“买牛奶”和“带雨伞”两条提醒，再读取结果完成总结；重复调度没有再次创建。
- 云端金额预算、其他 Provider、Skill 条件/补偿、隐私完整链、iOS 后台及真机、生产隔离和恢复验收仍保持待办。

本轮多轮 Agent 最终回归为 41 项通过，双提醒真实模型流程连续三次通过。0.6B 模型曾在首次回归中漏掉第二个事项，因此增加受预算约束的完成核对阶段；该验收不构成任意自然语言任务可靠完成的保证。


## 2026-09-17 真实 Docling 文档链路

- 独立 Python 3.12 环境安装 Docling 2.128.0 和 ONNX Runtime 1.30.0，完整依赖冻结；布局/表格/RapidOCR 的 22 个文件记录 SHA256，约 699 MB 权重不入库。
- 七格式真实 HTTP 测试通过：DOCX、Markdown、HTML、TXT、PPTX、PDF、PNG。测试覆盖设备签名上传、Core 加密文件、来源鉴权、真实解析、规范正文、跨成员/家庭拒绝、来源删除闭包。
- 实测修复 Multipart 预解析导致签名认证重复读取正文失败的问题，中间件保存原始摘要；篡改测试返回 401。
- Provider 凭据按成员登记，Registry 不再选择其他成员的秘密；网页增加上传并解析入口。
- Office 外部关系/实体、归档规模、图片像素、文档页数和输出大小受限。运行时离线配置不等同于生产操作系统沙箱；Linux 与恶意文件隔离验收仍待完成。


Docling 阶段完整回归：50 项通过、1 项失败。Docling 七格式均通过；失败来自 0.6B Agent 模型重复提出副作用，运行器正确阻止重复执行，但任务目标未完成。不得将这次回归报告为全绿。已开始下载官方固定版本 Qwen3-4B-Q4_K_M，校验完成后继续验证同一断言，未降低测试要求。


## 2026-09-17 Mem0 真实接入

- 固定 Mem0 1.0.11，在独立 Python 3.12 环境使用本地生成接口、真实 EmbeddingGemma 和本地 Qdrant；关闭遥测、禁用系统代理、限定 Python 出站目的地。
- 真实 Core 集成通过：规范账本重建、语义检索只回传规范 ID、授权账本回读、跨主体隔离、停用后保留账本、重新启用后删除传播。出站计数确认发生本地模型调用，没有未授权连接尝试。
- 修复固定 SDK history.reset 的嵌套锁死锁适配问题；清除使用分页循环，历史仅驻内存。未修改第三方源码。
- Consumer 在创建派生任务前核查 Provider 凭据归属，不为其他成员调用 Provider。
- Graphiti、生产操作系统隔离仍未验收。0.6B Agent 稳定性问题继续保留，4B 模型尚在下载，未以跳过测试宣称修复。

本轮选择性回归 51 项通过，明确未运行已知不稳定的 0.6B Agent 三次测试；该结果不是完整回归全绿。外部投影增加按成员手动重建接口与网页入口。


## 2026-09-17 Graphiti 准备

- SDK 固定到 0.30.2，已验证客户端构造；补齐未声明的 httpx 依赖。
- 新增独立 Neo4j Compose、随机凭据初始化、数据库认证检查及 Provider 启动脚本；镜像固定 OCI 摘要，数据卷独立，端口仅本机可访问。
- 客户端明确使用本地模型，关闭遥测和系统代理，禁用原始 episode 正文存储，增加 Python 出站目的地限制。
- 镜像已下载并固定摘要；Neo4j 5.26.30 认证连接、33 个 Graphiti 索引在线、真实主体清理隔离已通过。4B 权重仍在下载，模型实体抽取、语义搜索及 Core 全链路尚未通过。

派生消费者改为使用规范记录更新时间并按时间排序。相关 Mem0 实际回归与权限/门禁检查共 12 项通过。


## 2026-09-17 4B 与 Graphiti 完整模型链路

- 官方固定 4B 权重下载及 SHA256 校验通过，独立端口 58082 加载；原双提醒断言连续三次通过，没有降低成功要求。
- 实测定位到当前 llama.cpp 对嵌套 Schema 引用的兼容问题；适配层展开本地引用，保留必填字段并拒绝远程/循环引用。真实模型重新生成合法结构，不补造结果。
- Graphiti 已通过真实模型抽取、Neo4j 图搜索、Core 规范记录回读、跨主体隔离及删除传播测试；网络门禁未发现未授权连接尝试。
- 两个 Provider 已为本机管理员登记，索引 worker 更新为保留事实来源时间的实现。生产隔离、规模与复杂冲突质量仍未验收。


## 2026-09-17 whisper.cpp 真实语音转写

- 从官方固定提交构建 whisper.cpp 1.9.3，small-q5_1 多语言权重大小与 SHA256 校验通过。
- 真实 Core → 身份验证桥 → whisper.cpp → 加密任务结果链路通过英文人声与中文合成语音测试；自动语言识别另行覆盖。
- 增加 WAV 格式、采样率、声道、位宽、时长、截断和全零静音检查；不开放任意服务端文件路径或格式转换。
- 录音文本仍先交给用户编辑确认，不直接触发 Agent 操作。iOS 真机、实际麦克风/噪声、FunASR、CosyVoice 与生产沙箱仍未验收。

2026-09-18 最终本机完整回归 61 项通过，包含中英文显式/自动语言识别及全部已接入服务。未将合成中文语音当作真人或真机验收。


## 2026-09-18 设备同步与原生缓存

- 新增迁移 0004：设备快照、游标和批次确认表，FORCE RLS 与共享事件插入策略；SECRET 共享读取在数据库层同时拒绝。
- 数据事务顺序锁防止未提交事件被游标跳过，真实并发测试通过；共享接收成员能获取新增/撤权/删除通知。
- 快照固定版本、逐页重新鉴权，删除/撤权清除相关快照；批次重试不复活已删除来源，设备撤销清理同步状态。
- iOS 加密保存分页进度、缓存与待确认游标；连接切换前校验请求命名空间，上传保留服务器版本/分类。管理后台也使用完整分页。
- 真实模拟器 HTTPS + PostgreSQL 验收通过 205 条记录分页、重复上传确认、增量更新、删除、缓存恢复和设备撤销。5 项原生测试通过；首次无签名构建 Keychain -34018，使用临时签名后通过，没有降级到明文存储。
- 运行库迁移前生成并验证加密备份；将旧模式备份恢复到独立 homeai_restore_sync_upgrade_20260918，迁移及删除日志重放通过，未覆盖业务库。
- 完整 Python 回归 67 项通过。系统后台调度、APNs、真机与规模性能仍未完成。


## 2026-09-18 iOS 后台刷新接入

- 添加 SwiftUI appRefresh、BGTaskScheduler 标识与 fetch 模式；默认关闭，用户可主动开启/取消，不承诺固定唤醒间隔。
- 到期取消传递到真实同步流程，等待队列可取消且异常会释放；锁屏保护不可用时不降低密钥保护，前台恢复后重新读取连接并继续同步。
- 前后台凭据刷新串行化；原生测试通过真实 30 秒会话服务验证并发请求，不替换网络返回值。服务端拒绝用访问令牌签发新刷新凭据。
- 8 项原生测试通过，包括真实 205 条记录分页、上传、删除和撤销。系统真实调度、锁屏/低电量及 iPhone 真机仍待验收；已询问可用签名团队和目标设备。

本轮最终回归：69 项 Python 测试、8 项原生测试通过。后台系统唤醒没有被模拟器的通过结果替代，仍须真机验收。

签名认证新增非有限时间戳拒绝测试，阻止 NaN 绕过时间窗口；完整回归 69 项通过。


## 2026-09-18 文档检索业务闭环

- 新增迁移 0005 和文档分块向量表，真实本地 Embedding 建索引；正文按范围回到规范账本读取，不保存第二份明文片段。
- 真实 Docling 文件 → 语义检索 → 4B Agent 回答源文档时间与可信来源通过；共享/撤权/删除及旧任务结果隐藏通过。
- 检索和结果读取刷新 ORM 中已载入的记录，避免长事务拿旧版本或旧密级继续判断。
- iOS 改为实际 Multipart 文件上传与解析，来源链接再次鉴权；旧内联文件升级保留原 ID、版本关系和密级，提供授权原文件下载。
- 原生真实文件上传、解析、检索测试通过。索引规模、召回优化与完整隐私检测仍未验收。

本轮最终完整 Python 回归 74 项通过，原生 9 项通过，包含真实上传、Docling、向量检索和来源约束。迁移前加密备份已验证，运行库已升级 0005。


## 2026-09-18 FunASR 真实接入

- SDK 固定 1.4.14，torch/torchaudio 使用实际可用的匹配 2.11.0；独立环境和依赖锁已建立。
- 官方 SenseVoiceSmall 固定修订的五个模型文件校验通过；运行时用 SDK 内置实现、禁用远程代码及自动更新，Python 出站连接门禁保持关闭。
- 真实 Core 任务、模型和音频测试四项通过：中文/英文显式语言及自动识别。中文输入为明确标注的本机合成测试音频，英文为人声样本，不代表真机或全部语言验收。
- 模型加载和转写期间出站计数为零；请求不能指定远程模型或任意额外参数。
- 已登记本机管理员，whisper.cpp 保留；生产沙箱、更多语言和噪声场景仍待验证。

本轮完整回归 79 项通过，包括 FunASR 四项真实转写测试与此前所有已接入服务。

## 2026-09-18 事件自动化持久消费

- 新增迁移 0006：规范事件元数据、自动化触发配置、FORCE RLS 投递表和唯一约束。运行库升级前加密备份已校验。
- JetStream 持久 pull 消费者按规范 Outbox 校验事件，消费账本与投递同事务提交后 ACK；重投、并发调度不会重复创建任务。
- 数据新增/更新、删除、共享撤权可触发工作流；来源/类型过滤、共享显式开启、冷却排队和停用取消已实现。服务端因果链限制重复规则与四层深度。
- 五项真实 PostgreSQL/OPA/NATS 测试通过：真实事件创建提醒、提交后未 ACK 的重投、重复发布、并发消费与派发、冷却排队、停用、设备撤销、来源删除、共享撤权及跨成员投递隔离。
- 管理后台事件创建、查询投递与停用已通过真实浏览器和独立测试数据库验证，无横向溢出；没有用假响应代替服务。测试规则已停用。
- iOS 支持事件规则创建及事件名称展示，本轮编译通过；未把编译描述为真机或事件表单交互验收。
- 本机 API、任务 worker、记忆 worker 已恢复；HTTPS 健康检查正常，持久消费者历史积压和未确认消息均归零。

本轮完整 Python 回归 **84 项通过**，管理后台构建与 iOS 模拟器编译通过。Skill 条件/补偿、更多事件类型、规模与长时间故障演练仍未完成。

## 2026-09-18 声明式 Skill 条件

- Step 新增固定 when 契约：all/any、存在性、非空、标量相等和有限数值比较；只允许引用前序步骤，不执行脚本或模型判断。
- 条件不满足落库 SKIPPED 且不生成工具结果；恢复执行跳过该步骤，后续非法引用明确失败。条件满足仍使用原审批和策略路径。
- 保留旧无条件步骤的幂等摘要，不因新增可选字段导致旧请求重试冲突。
- 五项真实 PostgreSQL/OPA 测试覆盖跳过邮件发送、跨应用实例恢复、审批保持、非法前向引用/运算拒绝、缺失结果及 all/any/类型严格比较。
- 管理后台提供多步骤 JSON 配置和中文跳过状态说明；真实浏览器已创建并停用条件规则，未使用接口模拟。
- 完整 Python 回归 **89 项通过**；管理后台构建通过。本轮没有 iOS 代码修改，不重复声称真机验收。

补偿工作流尚未完成，已经发生的副作用不会因后续失败自动撤销。

## 2026-09-18 iOS 任务状态流

- 新增设备签名 WSS 单任务及最近任务状态流；握手复用时间窗和 nonce 防重放，每秒重新校验会话、设备及成员归属，撤销/过期断开已有连接。
- 快照仅含任务标识、状态和步骤进度；结果仍经正常任务接口检查来源权限。契约 TaskStateNotification 版本 1.0 已导出。
- iOS 聊天/语音任务等待改为状态通知，活动页自动更新审批和任务列表；任务详情仍读取真实授权结果。进入后台停止连接，前台重连以当前快照恢复。
- 服务端真实 PostgreSQL/OPA 测试覆盖任务执行通知、重连、无正文传输、跨成员拒绝、握手重放、设备撤销和令牌过期。
- 原生 9 项测试全部通过，其中真实 HTTPS/WSS 测试覆盖签名握手、实际创建提醒、完成通知、重新订阅及撤销后的关闭；未用假网络返回值替代验证。
- 首次完整回归 90 项通过、2 项因测试应用连接池未释放而失败。补齐工作流 fixture 与重启测试实例的连接池清理后，完整回归 **92 项通过**，没有提高数据库连接上限。
- 本机 API 已升级，临时测试服务停止，一次性配对文件已删除。

当前是任务状态流，模型逐 Token 文本流仍未完成。iPhone 真机、长时间弱网、网络切换、大规模连接及后台系统唤醒不在本次通过范围。
