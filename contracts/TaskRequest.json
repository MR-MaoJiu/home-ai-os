{
  "additionalProperties": false,
  "properties": {
    "message": {
      "default": "",
      "maxLength": 20000,
      "title": "Message",
      "type": "string"
    },
    "idempotency_key": {
      "maxLength": 200,
      "minLength": 8,
      "title": "Idempotency Key",
      "type": "string"
    },
    "mode": {
      "default": "local",
      "enum": [
        "local",
        "cloud"
      ],
      "title": "Mode",
      "type": "string"
    },
    "record_ids": {
      "items": {
        "type": "string"
      },
      "maxItems": 20,
      "title": "Record Ids",
      "type": "array"
    },
    "capability": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Capability"
    },
    "arguments": {
      "additionalProperties": true,
      "title": "Arguments",
      "type": "object"
    },
    "max_steps": {
      "default": 8,
      "maximum": 16,
      "minimum": 1,
      "title": "Max Steps",
      "type": "integer"
    },
    "max_output_tokens": {
      "default": 1024,
      "maximum": 4096,
      "minimum": 1,
      "title": "Max Output Tokens",
      "type": "integer"
    }
  },
  "required": [
    "idempotency_key"
  ],
  "title": "TaskRequest",
  "type": "object"
}